spring.application.name=phone-store-cms-web
server.port=8082
# Bind the embedded server to the loopback interface explicitly to avoid
# any issues with IPv6 or external interface binding when testing locally.
server.address=127.0.0.1
app.api.base-url=http://127.0.0.1:8081
